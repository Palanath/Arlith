package pala.apps.arlith.backend.streams;

public interface Determinate {
	/**
	 * Returns the number of bytes <b>remaining</b> in this {@link Determinate}.
	 * 
	 * @return The remaining number of bytes.
	 */
	int length();
}
